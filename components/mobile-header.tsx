import React from "react";

const MobileHeader = () => {
  return (
    <div className="lg:hidden h-[50px] px-6 fixed top-0 flex items-center justify-between bg-yellow-500 w-full z-50">
      Mobile Header
    </div>
  );
};

export default MobileHeader;
